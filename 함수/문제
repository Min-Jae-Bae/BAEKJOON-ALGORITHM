// 15596번 //
import java.util.Scanner;

public class Test {    // Test 클래스 생성
    long sum(int[] a) {
        long sum = 0;

        for (int i = 0; i < a.length; i++) {
            sum += a[i];
        }
        return sum; /// 합계를 나오게 한다.
    }
}
